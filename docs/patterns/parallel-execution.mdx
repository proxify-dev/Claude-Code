---
title: "Parallel Execution"
description: "Running multiple Claude sessions simultaneously — the single biggest productivity unlock."
---

## Why Parallel?

One agent handles one task at a time. While it's writing tests, it's not implementing the next feature. While it's refactoring, it's not fixing that bug.

The fix is simple: run multiple agents. This is the single biggest productivity unlock in agentic engineering.

## The Cascade

The workflow:

1. Open a new terminal tab for each task
2. Arrange tabs left-to-right in priority order
3. Kick off each agent with a clear, scoped prompt
4. Sweep left to right to review output as agents finish

<Warning>**Cap at 3-4 active sessions** — Beyond 3-4 active sessions, your context-switching cost as the reviewer starts outweighing the throughput gain. You become the bottleneck, not the agents.</Warning>

## Git Worktrees

The problem with parallel sessions: they all share the same working directory. Two agents editing the same files on different branches will collide.

**Git worktrees** solve this. A worktree is a branch checked out into its own directory. Each agent gets its own workspace.

**Without worktrees:**
```
my-project/          ← one directory, one branch at a time
```

**With worktrees:**
```
my-project/          ← main branch
my-project-feat-a/   ← feature-a branch, its own directory
my-project-feat-b/   ← feature-b branch, its own directory
```

You don't need to memorize the commands. Just ask Claude: "Create a worktree for feature X." It handles the rest.

<Tip>Each worktree is a full working directory. You can run dev servers, tests, and builds independently in each one.</Tip>

## Shell Aliases for Rapid Switching

Set up short aliases to jump between worktrees and launch sessions fast:

```bash
# Quick directory jumps
alias za="cd ~/projects/my-app"
alias zb="cd ~/projects/my-app-feat-a"
alias zc="cd ~/projects/my-app-feat-b"

# Quick Claude launches
alias c="claude"
alias ch="claude --with-chrome"  # Claude with browser MCP
```

Small friction reductions compound when you're switching between 3-4 sessions throughout the day.

## How Many Is Too Many?

| Sessions | Verdict |
|----------|---------|
| 1 | Baseline — you're serializing work |
| 2-3 | Sweet spot for most engineers |
| 4-5 | Effective if tasks are independent and well-scoped |
| 6+ | Diminishing returns — you can't review fast enough |

The constraint isn't compute. It's your ability to review and integrate the output.

## Worktree Isolation on Agents

Beyond manual worktrees, Claude Code can create worktrees automatically when spawning agents. Set `isolation: "worktree"` in agent frontmatter:

```yaml
---
name: refactor-agent
isolation: worktree
background: true
---
```

Each agent gets its own branch, working directory, and git index. If the agent makes no changes, the worktree is auto-cleaned on session end. If changes exist, you're prompted to keep or remove.

<Tip>Add `.claude/worktrees/` to your `.gitignore`. Each new worktree may need its own dependency installation (`npm install`, etc.).</Tip>

## The `/batch` Command

The `/batch` command automates fan-out with worktree isolation. Give it a migration instruction and it:

1. Decomposes the work into 5-30 self-contained units
2. Spawns one background agent per unit in its own worktree
3. Each agent implements, tests, commits, and opens a PR
4. Renders a status table as agents complete

**Best for:** convention enforcement, dependency swaps, code migrations across many files — any work where agents don't need to communicate.

This is dramatically cheaper than [Agent Teams](agent-teams/) because it eliminates inter-agent coordination overhead entirely.

## Combining with Session Management

Each worktree should be self-contained:

- Its own Claude session
- Its own `HANDOFF.md` if you need to pause and resume
- Its own branch with regular commits

When an agent finishes in one worktree, review the output, merge the branch, and clean up the worktree. Then spin up a new one for the next task.

## Going Deeper

For more sophisticated multi-agent coordination, see:
- [Orchestration Patterns](orchestration-patterns/) — fan-out, pipelines, builder-validator, and more
- [Agent Teams](agent-teams/) — multi-session collaboration with shared task lists
- [Cost & Model Routing](cost-and-model-routing/) — controlling token costs across parallel workflows

---

**← Prev:** [Session Management](session-management/) · **Next:** [Prompting Craft →](prompting-craft/)
